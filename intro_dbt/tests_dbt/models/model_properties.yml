version: 2

# Replace any entry of check_gt_0 with the appropriate content
models:
- name: taxi_rides_raw
  columns:
    - name: fare_amount
      tests:
        - check_gt_0
    - name: total_amount # field_name
      tests:
        - check_gt_0

# second example
version: 2

models:
- name: taxi_rides_raw
  columns:
    - name: tpep_pickup_datetime
      tests:
        - columns_equal:
            column_name2:  tpep_dropoff_datetime

